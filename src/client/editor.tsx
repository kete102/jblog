/**
 * Client-side editor entry point.
 * This bundle is loaded only on dashboard editor pages.
 * Tiptap mounts into #editor-root once the DOM is ready.
 * Full implementation is wired up in Phase 3.
 */

// Placeholder — replaced in Phase 3 with full Tiptap integration
console.debug('[jblog] editor bundle loaded')
